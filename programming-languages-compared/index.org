#+Title: Programming Languages Compared

* arrays
** Basico do basico
+ criar um array
+ incluir item
+ retirar item
+ iterar no array
  

#+name: array in shellscript
#+begin_src shell
  stack1_containers=('fzl-portainerfzl-portainer' 'fzl-mysql'  'fzl-postgresql' 'fzl_nginx' 'fzl_php8.3-fpm')
  echo ${stack1_containers[0]}
  
  for i in "${stack1_containers[@]}"; do
      echo "$i"
  done
#+end_src

#+RESULTS: array in shellscript
| fzl-portainerfzl-portainer |
| fzl-portainerfzl-portainer |
| fzl-mysql                  |
| fzl-postgresql             |
| fzl_nginx                  |
| fzl_php8.3-fpm             |


* strings concatenation

#+BEGIN_TABS 

#+name: python: concatena̤̣o com operador "+"
#+begin_src python :session s1 :shebang "#!/usr/bin/env python3" :exports both :results output
  primeiro_nome="Ada"
  sobrenome="Lovelace"
  idade=28
  data_ano=1843
  print("O primeiro algoritmo foi projetado pela princesa " + primeiro_nome + " " + sobrenome + " com " + str(numerointeiro) + " anos de idade, em " + str(data_ano))
#+end_src

#+RESULTS: python: concatena̤̣o com operador "+"
: O primeiro algoritmo foi projetado pela princesa Ada Lovelace com 3 de idade, em 1843


#+name: python: concatena̤̣o com f print
#+begin_src python :session s1 :shebang "#!/usr/bin/env python3" :exports both :results output
  primeiro_nome="Ada"
  sobrenome="Lovelace"
  idade=28
  data_ano=1843
  print(f'O primeiro algoritmo foi projetado pela princesa {primeiro_nome} {sobrenome} com {numerointeiro} anos de idade, em {data_ano}')
#+end_src

#+RESULTS: python: concatena̤̣o com f print
: O primeiro algoritmo foi projetado pela princesa Ada Lovelace com 3 anos de idade, em 1843



#+name: javascript: concatena̤̣o com concat
#+begin_src elisp 
  (setq primeiro_nome "Ada")
  (setq sobrenome "Lovelace")
  (setq idade 28)
  (setq data_ano 1843)
  (message (concat "O primeiro algoritmo foi projetado pela princesa " primeiro_nome " " sobrenome " com " idade " anos de idade, em " data_ano))
#+end_src

#+END_TABS











* Refs
https://levelup.gitconnected.com/30-python-concepts-i-wish-i-knew-way-earlier-3add72af6433
